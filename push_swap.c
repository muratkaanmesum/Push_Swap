#include "libft/libft.h"
#include "push_swap.h"
#include <stdio.h>
#include <stdlib.h>

int	main(int argc, char *argv[])
{
	int		*arr;
	int		i;
	t_stack	*stack_a;
	t_stack	*stack_b;
	int		value;

	i = 0;
	arr = malloc(sizeof(int) * argc);
	get_args(argc, argv);
	return (0);
}
